import RPI.GPIO as GPIO
from time import sleep 
dtoverlay = gpio-shutdown
ledpin = 4
pushpin = 17
GPIO.setup($SYS_IS_RS
echo "Power off?"
GPIO.setup(pushpin, GPIO.IN, pull_up_down=GPIO.PUD_UP)
GPIO.setup(4,GPIO.OUT, Initial = GPIO.low)
sudo poweroff
fi